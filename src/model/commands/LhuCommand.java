package model.commands;

import model.Core;
import model.Register;

/**
 * Created by ncameron on 4/27/2017.
 */
public class LhuCommand extends Command {
    public LhuCommand(Core core, Register dst, int memLocation) {
        super(core);
    }}
