package model.commands;

import model.Core;

/**
 * Created by ncameron on 4/27/2017.
 */
public class AddiuCommand extends Command {
    public AddiuCommand(String arguments, Core core) {
        super(arguments, core);

        this.function = s -> {

        };
    }
}
